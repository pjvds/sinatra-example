box: wercker/ruby
build:
    steps:
        - bundle-install
deploy:
    steps:
        - openshift-deploy:
            private_key: $WERCKER_OPENSHIFT_PRIVATE_KEY
            git_url: $WERCKER_OPENSHIFT_GITURL
